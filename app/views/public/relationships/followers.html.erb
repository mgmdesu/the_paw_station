<div class="container">
  <div class="row">
    <div class="mx-auto">
      <h2>Followed Users</h2>
        <% if @users.exists? %>
          <%= render 'public/users/relationships', users: @users %>
        <% else %>
          <p>フォローされているユーザーはいません</p>
        <% end %>
    </div>
  </div>
</div>