<% if walk.bookmarked_by?(current_user) %>
  <p>
    <%= link_to walk_bookmarks_path(walk), method: :delete do %>
      <i class="fas fa-paw"></i> ブックマーク中
    <% end %>
  </p>
<% else %>
  <p>
    <%= link_to walk_bookmarks_path(walk), method: :post do %>
      <i class="fas fa-paw"></i> ブックマークする
    <% end %>
  </p>
<% end %>